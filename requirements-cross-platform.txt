# Windows / Linux (and optional non-MLX macOS) provider stack.
# FFmpeg must also be installed on the system PATH.
yt-dlp[default,curl-cffi]
faster-whisper>=1.1,<2
demucs>=4.1.0,<5
numpy>=1.26
soundfile>=0.12
piper-tts
